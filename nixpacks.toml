[phases.setup]
nixPkgs = ["nodejs_22", "bun"]

[phases.install]
cmds = [
  "bun install",
  "bunx prisma generate"
]

[phases.build]
cmds = [
  "bunx prisma generate && bun run build"
]

[start]
cmd = "bunx next start -H 0.0.0.0"

[variables]
NODE_ENV = "production"

